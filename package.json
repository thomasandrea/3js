{
  "name": "monorepo",
  "private": true,
  
    "scripts": {
      "build": "npm run build:bitmama && npm run build:morphing && npm run copy:index",
      "build:bitmama": "cd projects/bitmama && vite build",
      "build:morphing": "cd projects/morphing && vite build",
      "copy:index": "cp index.html dist/index.html"
    },
  "dependencies": {},
  "devDependencies": {
    "vite": "^5.4.11"
  }
}
